{
    "name": "@abap/audit-core",
    "version": "0.1.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "exports": {
        ".": "./src/index.ts"
    },
    "scripts": {
        "build": "tsc",
        "lint": "eslint src/",
        "type-check": "tsc --noEmit",
        "test": "vitest run",
        "clean": "rimraf dist .turbo"
    },
    "dependencies": {
        "@abap/types": "workspace:*",
        "@abap/constants": "workspace:*"
    },
    "devDependencies": {
        "rimraf": "^6.0.1",
        "vitest": "^2.1.8"
    }
}